# AGENT.md

## 原则

1. 开发代码规范应尽量与 Qt 官方风格和实践保持一致。
2. 所有 `qsTr()`、`tr()` 以及其他界面翻译文本接口中的源文本必须使用英文。

## 说明

* 命名、接口设计、头源文件组织、注释风格和 Qt 对象使用方式，优先参考 Qt 官方代码风格。
* 面向用户展示的文本允许被翻译为其他语言，但代码中的翻译源字符串必须统一写成英文。
* 新增 UI 文本时，不要直接把中文写入 `qsTr()` 或 `tr()`。
