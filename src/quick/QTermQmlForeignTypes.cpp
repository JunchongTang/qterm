// Reserved for future QML_FOREIGN proxy registrations if needed.
// QTermTerminal and QTermSurfaceModel are intentionally NOT registered here:
// they are exposed as QObject* properties and accessed via Qt's meta-object
// system — no explicit QML type name is required for property traversal.
